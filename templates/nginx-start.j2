#!bin/bash

# start.sh
# Generated by Ansible for {{ansible_fqdn}}
NGINX_PREFIX={{nginx_install_prefix}}

if [[ ! -f /etc/init.d/nginx ]]; then
  $NGINX_PREFIX/sbin/nginx -c $NGINX_PREFIX/conf/nginx.conf
else
  service nginx start
fi;
